{
  "name": "thumbor-nginx",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "yarn run jasmine",
    "dev:test": "yarn run nodemon --ext js --ignore node_modules --exec 'yarn test'"
  },
  "devDependencies": {
    "jasmine": "^3.3.1",
    "nodemon": "^1.18.10",
    "unirest": "^0.5.1"
  }
}
